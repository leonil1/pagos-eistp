<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<AnchorPane xmlns="http://javafx.com/javafx/17.0.12"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.iestpdj.iestpdjpagos.controller.DashboardController"
            prefWidth="1038.0" prefHeight="898.0"
            styleClass="root">

    <children>
        <SplitPane fx:id="sideSplitPane"
                   dividerPositions="0.25"
                   AnchorPane.topAnchor="0.0"
                   AnchorPane.bottomAnchor="0.0"
                   AnchorPane.leftAnchor="0.0"
                   AnchorPane.rightAnchor="0.0">

            <items>
                <!-- Menú lateral -->
                <VBox fx:id="sideMenu" spacing="10" styleClass="side-menu">
                    <children>
                        <Label text="MENÚ" styleClass="menu-title" />
                        <Button fx:id="btDashboard" text="👤 Dashboard" styleClass="side-button" />
                        <Button fx:id="btnUsuario" text="👤 Usuario" styleClass="side-button" />
                        <Button fx:id="btnEstudiantes" text="🎓 Estudiantes" styleClass="side-button" />
                        <Button fx:id="btnPagos" text="💰 Pagos" styleClass="side-button" />
                        <Button fx:id="btnReportes" text="📄 Reportes" styleClass="side-button" />
                    </children>
                </VBox>

                <!-- Contenido principal -->
                <VBox fx:id="mainContent" styleClass="main-content" spacing="10" alignment="TOP_LEFT">
                    <children>
                        <Label fx:id="textBienvenido" text="Información de usuario" styleClass="label-titulo" />
                        <Label fx:id="textUsuario" text="Bienvenido" styleClass="label-subtitulo" />
                    </children>
                </VBox>
            </items>
        </SplitPane>
    </children>
</AnchorPane>
